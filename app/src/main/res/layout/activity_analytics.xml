<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v4.widget.SlidingPaneLayout
            android:id="@+id/activity_main_sliding_pane"
            android:layout_width="match_parent"
            android:layout_height="match_parent">


            <include layout="@layout/drawer_list" />

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:elevation="@dimen/drawer_elevation"
                    android:orientation="vertical">

                    <include layout="@layout/toolbar" />

                    <include
                        android:id="@+id/today_analytics"
                        layout="@layout/today_analytics" />

                    <include
                        android:id="@+id/chart_card"
                        layout="@layout/chart_card" />

                    <include
                        android:id="@+id/top"
                        layout="@layout/product_top" />

                    <include
                        android:id="@+id/expiring"
                        layout="@layout/expiring_products" />
                </LinearLayout>
            </ScrollView>
        </android.support.v4.widget.SlidingPaneLayout>
    </LinearLayout>
</layout>